专业术语：
SE-io
1、对象序列化、数据持久化
对象流连接中两个流的工作分别为：
 1）：先将要写出的对象交给对象流写出，而
    对象流会根据该对象结构将其转化为一组
    字节，这个过程为：对象序列化
 
 2）：对象流进行序列化后，会将这组字节经过
    其连接的文件流最终写入到文件中。
    而将数据写入文件(硬盘上)的过程称
    为：数据持久化

2、对象反序列化
对象输入流的读取对象方法是将读取的字节
在按照其表达的结构将其还原为对象，而这个
过程为:对象反序列化


